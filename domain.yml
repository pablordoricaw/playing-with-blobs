version: '2.0'
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true

intents:
- faq:
    use_entities: ["name"]
- chitchat:
    use_entities: ["name", "email", "phone number"]